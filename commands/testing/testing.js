const { MessageEmbed } = require('discord.js')

module.exports = {
    name: "testing",
    aliases: ["test"],
    cooldown: 1000 * 250,
    description: "To test something",
    async execute(client, message, args) {
        
    }
}